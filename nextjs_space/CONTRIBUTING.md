
# Руководство по внесению вклада в проект

Спасибо за интерес к улучшению образовательной платформы SSV Наука! Мы ценим любой вклад в развитие проекта.

## 📋 Содержание

- [Кодекс поведения](#кодекс-поведения)
- [Как начать](#как-начать)
- [Процесс разработки](#процесс-разработки)
- [Стандарты кода](#стандарты-кода)
- [Процесс Pull Request](#процесс-pull-request)
- [Сообщения о проблемах](#сообщения-о-проблемах)

## 🤝 Кодекс поведения

Участвуя в этом проекте, вы соглашаетесь соблюдать уважительное и профессиональное поведение. Мы создаем открытую и дружелюбную среду для всех участников.

## 🚀 Как начать

### Требования

- Node.js 18+ и Yarn
- PostgreSQL 14+
- Git
- Базовые знания Next.js, React, TypeScript

### Настройка локального окружения

1. **Форкните репозиторий**
   ```bash
   # Нажмите кнопку "Fork" на GitHub
   ```

2. **Клонируйте ваш форк**
   ```bash
   git clone https://github.com/ВАШ_USERNAME/gastric-surgery-course.git
   cd gastric-surgery-course/nextjs_space
   ```

3. **Установите зависимости**
   ```bash
   yarn install
   ```

4. **Настройте переменные окружения**
   ```bash
   cp .env.example .env
   # Заполните необходимые значения в .env
   ```

5. **Инициализируйте базу данных**
   ```bash
   yarn prisma generate
   yarn prisma db push
   yarn prisma db seed
   ```

6. **Запустите dev сервер**
   ```bash
   yarn dev
   ```

## 💻 Процесс разработки

### Создание новой ветки

Всегда создавайте отдельную ветку для ваших изменений:

```bash
git checkout -b feature/название-функции
# или
git checkout -b fix/название-бага
```

### Соглашение об именовании веток

- `feature/` - новые функции
- `fix/` - исправления багов
- `docs/` - изменения в документации
- `refactor/` - рефакторинг кода
- `test/` - добавление или изменение тестов
- `style/` - форматирование, отсутствующие точки с запятой и т.д.

### Примеры названий веток

```
feature/add-quiz-component
fix/authentication-redirect
docs/update-readme
refactor/simplify-api-routes
```

## 📝 Стандарты кода

### TypeScript

- Используйте TypeScript для всех новых файлов
- Избегайте использования `any`, предпочитайте строгую типизацию
- Используйте интерфейсы для объектов

### Стиль кода

- Используйте 2 пробела для отступов
- Используйте одинарные кавычки для строк
- Добавляйте точку с запятой в конце выражений
- Используйте `const` по умолчанию, `let` только при необходимости

### React компоненты

```typescript
// ✅ Хорошо
export function MyComponent({ title }: { title: string }) {
  return <div>{title}</div>
}

// ❌ Плохо
export default function ({ title }) {
  return <div>{title}</div>
}
```

### Импорты

Порядок импортов:
1. React и Next.js
2. Внешние библиотеки
3. Компоненты проекта
4. Утилиты и типы
5. Стили

```typescript
import { useState } from 'react'
import Link from 'next/link'
import { Button } from '@radix-ui/react-button'
import { Header } from '@/components/header'
import { cn } from '@/lib/utils'
import styles from './styles.module.css'
```

### Коммиты

Используйте [Conventional Commits](https://www.conventionalcommits.org/):

```
feat: добавлен интерактивный компонент 3D анатомии
fix: исправлена ошибка загрузки видео
docs: обновлена документация API
style: форматирование кода
refactor: упрощен компонент навигации
test: добавлены тесты для модуля аутентификации
chore: обновлены зависимости
```

## 🔄 Процесс Pull Request

### Перед созданием PR

1. ✅ Убедитесь, что код компилируется без ошибок
2. ✅ Запустите линтер: `yarn lint`
3. ✅ Проверьте, что все существующие функции работают
4. ✅ Добавьте или обновите документацию при необходимости

### Создание Pull Request

1. **Обновите вашу ветку**
   ```bash
   git checkout main
   git pull upstream main
   git checkout ваша-ветка
   git rebase main
   ```

2. **Отправьте изменения**
   ```bash
   git push origin ваша-ветка
   ```

3. **Создайте PR на GitHub**
   - Перейдите в оригинальный репозиторий
   - Нажмите "New Pull Request"
   - Выберите вашу ветку
   - Заполните шаблон PR

### Шаблон описания PR

```markdown
## Описание
Краткое описание изменений

## Тип изменений
- [ ] Новая функция
- [ ] Исправление бага
- [ ] Документация
- [ ] Рефакторинг

## Чеклист
- [ ] Код компилируется без ошибок
- [ ] Линтер проходит успешно
- [ ] Документация обновлена
- [ ] Ручное тестирование выполнено

## Скриншоты (если применимо)
```

## 🐛 Сообщения о проблемах

### Создание Issue

При обнаружении бага или предложении новой функции:

1. Проверьте, нет ли уже похожей issue
2. Используйте понятный заголовок
3. Опишите проблему детально
4. Приложите скриншоты или видео
5. Укажите шаги для воспроизведения

### Шаблон Bug Report

```markdown
**Описание бага**
Четкое описание проблемы

**Шаги для воспроизведения**
1. Перейти на '...'
2. Нажать на '...'
3. Увидеть ошибку

**Ожидаемое поведение**
Что должно происходить

**Скриншоты**
Если применимо

**Окружение**
- Браузер: [Chrome, Firefox, Safari]
- Версия: [89, 90]
- ОС: [Windows, macOS, Linux]
```

## 🎯 Приоритетные области для вклада

- 🧪 Написание тестов для существующих компонентов
- 📚 Улучшение документации
- 🎨 Улучшение UI/UX
- ♿ Улучшение доступности (a11y)
- 🌍 Добавление переводов
- 🔧 Оптимизация производительности

## 📞 Контакты

Если у вас есть вопросы:
- Создайте Issue на GitHub
- Напишите в Discussions

## 📄 Лицензия

Внося вклад в этот проект, вы соглашаетесь с тем, что ваши изменения будут лицензированы под MIT License.

---

**Спасибо за ваш вклад! 🎉**
